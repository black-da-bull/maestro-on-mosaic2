# Folder contract — .claude/skills/maestro-forensic-transcript/  (M19)

- **Axis:** ROLE = installed Agent Skill (tooling). Not evidence, not runtime canon.
- **Domain:** reconstruction method (transcript → architecture). NOT music generation.
- **Rung:** persisted artifact (files). The *content* is RECONSTRUCTED, not operator-authored — found originals outrank it on discovery (DEC-03 / O-03). This install is the loadable copy; the SSOT of record is `_PROVENANCE/`.
- **Version:** v1.1 (2026-07-19 fold), packaging-repaired 2026-07-20 (frontmatter restored, spec-compliant zip). Supersedes the v1.0 skeleton by pointer; v1.0 preserved verbatim as `SKILL_v1_ORIGINAL.md`. See LEDGER `SKILL-REPKG-1`.

## What belongs here
The self-contained skill package: `SKILL.md` (frontmatter + body), `00`–`06`, `patterns/`, `python/`, `examples/`, `MANIFEST.yaml`, `README_FIRST.md`, `SKILL_v1_ORIGINAL.md`, and the point-in-time `state_snapshot_2026-07-19/`.

## What must NEVER be placed here
- Live doctrine. The `state_snapshot_*` copies are frozen ATP travel copies, not truth — on any conflict, `_PROVENANCE/STATE.md · OPEN.md · LEDGER.md` win (per each snapshot's `SNAPSHOT_NOTE.md`). Never edit a snapshot to "fix" state; fix the SSOT.
- Runtime canon (UST/SEM/SEG/FOIL). This is method, a separate register.

## Who may write
Skill maintenance only, on a new fold (supersede-by-pointer, never rewrite in place). Re-fold checklist item: **preserve the YAML frontmatter** — the v1.0→v1.1 fold dropped it, which is what broke the claude.ai upload (LEDGER `SKILL-REPKG-1`).
