---
description: Orient this session to the Maestro-on-Mosaic goal, verified against live STATE.
---

# /goal — orient to the mission, then check where we actually are

You are beginning (or re-centering) a Maestro-on-Mosaic session. Do this, in order:

1. **Load the canonical goal.** Read `_PROVENANCE/GOAL.md` in full. It is operative canon —
   the north-star (the soul), the three stacked goals (Recover → Externalize → Never re-thin),
   the laws, and the failure path.

2. **Load live state.** Read `_PROVENANCE/STATE.md` (current ordering) and `_PROVENANCE/OPEN.md`
   (unresolved contradictions). STATE is authoritative for the live objective; GOAL.md §4 is only
   as current as the last time it was synced.

3. **Report back — short — in this shape:**
   - **North-star:** one line (the soul; scaffolding serves it, never the reverse).
   - **Active gate:** which of the three goals we're in right now, and the top next action from STATE.
   - **Drift check:** does GOAL.md §4 still match STATE's live ordering? If not, say so and flag it —
     do not silently reconcile.
   - **Open hazards:** the 1–3 OPEN items most relevant to the next action.

4. **Then stop and confirm the next action with the operator** before doing work.
   Interactive gates, never passive stops (M12). Freeze nothing; verdict nothing until asked (M18).

Constraints while running this command: do not summarize evidence in place, do not dedup by name,
do not promote your own output to canon. ROOT (operator) outranks everything you produce here.
