# P1 Evidence Recovery — Poster Two / Calibrate + Run It To Me

Doctrine: **Your Vision. Our Mission.**

Status: **RECOVERED EMPIRICAL WORKSPACE EVIDENCE — COMPARATIVE RENDER LINEAGE PRESENT — B3-SPECIFIC RESULT STILL OPEN.**

## Evidence authority and scope

This note preserves two separate empirical work windows without flattening their different evidentiary roles:

1. **Poster Two / Calibrate** — an analyzed audio artifact with preserved technical measurements and downstream production use.
2. **Run It To Me** — an operator-established renderer lineage that began as a cover and then moved into fresh rendering under **four distinct style prompts**.

These sources strengthen the empirical renderer record, but neither may be silently inflated into a current-v5.5 Hammond B3 / Leslie PASS unless B3-specific audible evidence can actually be tied to the relevant render.

The distinction is evidence scope, not evidence quality.

## Evidence window A — Poster Two / Calibrate

The existing workspace evidence used for empirical grounding includes the analyzed audio artifact:

`Calibrate (Cover) (Cover) (Instrumental).mp3`

Do not require the operator to re-download this artifact merely to re-prove the general renderer/output premise. The workspace record already preserves the artifact analysis and its relationship to the Poster Two work.

### Recovered empirical observations

The preserved workspace analysis records, for the audio artifact above:

- duration approximately 2:55.3;
- 48 kHz stereo source;
- integrated loudness approximately -16.4 LUFS;
- loudness range approximately 4.8 LU;
- true peak approximately +0.1 dBTP;
- tempo candidates centered around approximately 152 BPM with a roughly 75 BPM halftime interpretation;
- estimated tonal center around F#;
- low/low-mid dominant spectral balance;
- detected structural change points and a usable section map.

The same work window then uses that analyzed bed for the Poster Two vocal/performance plan and downstream fit review.

### What Poster Two / Calibrate supports

This is admissible empirical project evidence that:

1. the Maestro/Suno workflow produced or preserved a real audio artifact that was brought back into the workspace for analysis;
2. the artifact was measurable rather than hypothetical prompt text;
3. the workspace used measured audio behavior to drive subsequent arrangement, performance, engineering and crossstream decisions;
4. general renderer/output reality must not be reopened as if no empirical audio evidence exists.

## Evidence window B — Run It To Me

### Operator-root lineage correction

The operator has now restored the missing chronology:

1. **Run It To Me began as a cover.**
2. The work then moved beyond the cover lineage into **fresh renders**.
3. The fresh-render phase used **four different style prompts**.

Do not collapse these into one generation mode. The cover stage and the four-style fresh-render stage are distinct points in the experiment history.

### Workspace corroboration already recovered

The indexed workspace preserves a later `Run It To Me` control surface showing:

- a processed vocal source named `Run It To Me (Vocals)`;
- source duration shown as 7:09;
- a selected vocal region from approximately 02:30.5 to 03:06.8;
- a v5.5 Voice named `Harry & The Juke Joints v5.5` based on that source;
- the surrounding Person / Lyrics Prompt / Styles Prompt triad context.

This corroborates that Run It To Me became an operative source inside the v5.5 renderer-facing workflow rather than remaining only an abstract prompt example.

### What the four-style lineage supports now

The operator-root chronology is sufficient to preserve the following as an active recovery fact:

- Run It To Me contains a **comparative renderer work window** in which the project intentionally changed style-prompt inputs across four distinct prompts after the initial cover phase.

That makes Run It To Me more informative than a single-render existence proof: it can potentially reveal which audible behaviors are stable across prompt changes and which are prompt-sensitive.

However, the indexed evidence recovered so far does **not** expose the exact four prompt bodies, their render IDs/URLs, or criterion-level listening observations. Those identities remain recovery targets and must not be invented.

### What remains to recover from Run It To Me

Recover, in this order, if present in the workspace or directly accessible renderer history:

1. the four style-prompt texts or stable prompt identifiers;
2. the render IDs/URLs associated with each prompt;
3. whether each prompt produced one or more siblings;
4. direct listening notes or preserved operator judgments tied to those renders;
5. whether Hammond B3 / Leslie behavior was present, instructed, intentionally varied, or held constant in any of those runs;
6. enough timestamped or section-level observations to score any applicable B3 rubric criteria.

Do **not** request fresh downloads merely because these fields are not yet indexed in the project text.

## What the combined evidence does not yet support

Poster Two / Calibrate plus Run It To Me do **not** automatically establish:

- current-v5.5 B3 instrument identity consistency;
- current-v5.5 Leslie/rotary consistency;
- section-by-section B3 restraint, entrance, response, bloom, swell/release or decay;
- a B3-specific PASS.

Run It To Me can close some or all of that gap **only if** the recovered four-style prompt/render lineage contains audible B3/Leslie evidence that maps to the calibration rubric.

## Download-conservation rule

The operator has limited downloads. Therefore:

1. reuse preserved workspace evidence first;
2. recover the Run It To Me four-style prompt identities and existing Suno song URLs/IDs or listening notes before requesting any export;
3. do not require re-download of Poster Two / Calibrate merely for provenance;
4. do not require re-download of Run It To Me renders merely to prove the operator-established comparison occurred;
5. score directly accessible renders by listening before exporting audio;
6. download/export audio only when a criterion cannot be judged from preserved notes or direct renderer access and local signal analysis is actually required;
7. if Run It To Me supplies adequate B3-specific evidence, use it before generating a new B3 comparison;
8. only if the recovered evidence still cannot answer the B3 question, generate the smallest missing current-v5.5 sibling comparison;
9. run a second reuse-prompt sibling pair only if the first necessary fresh pair is split, ambiguous, confounded, or insufficient to judge consistency.

This supersedes the prior assumption that four newly downloaded raw renders are automatically required.

## Current P1 state

- general empirical renderer/output evidence: **SUPPORTED** by Poster Two / Calibrate and the broader project render record;
- comparative style-prompt renderer lineage: **SUPPORTED AT OPERATOR-ROOT CHRONOLOGY LEVEL** by Run It To Me — cover first, then fresh rendering with four distinct style prompts;
- exact Run It To Me four-prompt texts + render IDs/URLs + criterion observations: **RECOVERY OPEN**;
- current-v5.5 B3/Leslie baseline: **OPEN PENDING RECOVERY**;
- mandatory fresh four-render download set: **SUPERSEDED**;
- next action: **recover the Run It To Me four-style render identities and inspect their audible evidence before authorizing any fresh B3 generation.**

No experiment result is promoted into Maestro canon by this evidence-recovery note.