# MINIMAL KNOWLEDGE

Vocabulary anchors and load-bearing references. Not corpus expansion. Not explanation.

## Substrate Origin

LLM systems compress destructively between reasoning and surfaced output. Substrate = the material compressed away in that move. Observed empirically (operator read Claude's extended reasoning against Claude's emitted response). Origin observation, `OPERATOR_ASSERTED`, load-bearing.

## Anti-Summarization Forbidden Patterns

Runtime refuses output containing:

- "This section remains as previously defined"
- "Same as above"
- "As stated earlier"
- "Unchanged"
- "Etc."
- "And so on"
- "Previously covered"
- "See above"

If a section is large, emit it large. Compression for brevity destroys substrate.

## Operator Cognitive Architecture (operative parameter, not biography)

- 256-lane parallel processing internally; 4-lane output (2 eyes + 2 ears + 1 mouth)
- 64:1 compression ratio on emit
- Z-to-A processing — starts at destination, works backward
- Bottom-up accumulation, not top-down deduction
- Inference is contrastive, not deductive (probe → response → map)
- Compression in operator's emit IS the discipline; AI matches it on emit, does not expand against it

## Five-Layer Pre-Loader (v4.5.5 staging) — [STAGING-EMITTED] [OPERATOR-CONFIRMED]

```
L1  MONOLITHIC PROMPT          KERNEL / CONSTITUTION
L2  KNOWLEDGE BASE             MODULAR OPERATIONAL MEMORY
L3  KNOWLEDGE + HEURISTICS     TACTICAL STRATEGY / LEARNED EXPERIENCE
L4  HISTORICAL LINEAGE         EVOLUTIONARY MEMORY / ANTI-REGRESSION
L5  blueprint.json             ACTIVE PROJECT STATE
```

L1 is highest authority. L5 is project source of truth, NOT OS source of truth. Pre-loader = continuity restoration mechanism, not setup ceremony.

## Seven Workspaces as Sequential Epistemic Phases

1. v5 — daydream
2. v5-b — salvage
3. v5-c — substrate-recognition
4. forensic — investigation
5. claude (prior) — diagnosis (teleological loss named; substrate ideated)
6. claude (2nd prior) — pending operator characterization
7. current — final consolidation

Sequential phases, not parallel runtime attempts. Discovery value high and cross-validated even where runtime-continuation fails.

## Lineage Anchor

v2.6 → v3.x → v4.x → v4.5.x (WORKING ROOT) → v5 / v5-b / v5-c (exposes_failure layers) → Mosaic Engine v0.1 + Maestro v0 (CANDIDATE_SPEC)

v4.5x = operational root. v5 / v5-b / v5-c = diagnostic / stress-test / discovery layers, NOT destination canon.

## Load-Bearing Architectural Facts

- **SEM** — 12-criterion at 97.5% release threshold per CR-009. Calibrated to operator HPA / vagal response. NOT arbitrary. The body's threshold for "this is alive."
- **Sacred Imperfection** — INV-09 kernel invariant. Sterile production fails HPA gate. Required for release, not optional.
- **Technical UST** — 8 axes with state machine NULL → PROPOSED → PRESSURED → RESOLVED → LOCKED. Reverse compilation blocked until at least one axis hits LOCKED.
- **Triad output** — Performer Profile (≤2000 chars) + Show Summary (≤1000 chars) + Session Sheet / Creative UST (4950-4995 chars). Training signal, not output.
- **FOIL** — governs lawful promotion and deduplication only. NOT repeat notation. NOT the whole of reverse processing.
- **ATP** — Air-Gapped Transfer Pack. Verifiable, resumable, auditable session export. Hash-verified state capsule any future session can resume from.

## Forbidden Reconstruction Targets

Runtime refuses to reconstruct content for: Q1-Q16, GAP-02, GAP-06, GAP-08, GAP-09, VIS/K8, +4 governors, mixed-use, HPA aggregate, GAP-EXEC. These are canon-blocking gaps. Referenced as open. Not closed by inference.

## Vocabulary Anchors

- **plane** in this runtime = one of the four coupled systems (Maestro / Mosaic / Dev_Environment / Forensics). NOT a layer of one stack.
- **Plane-4 surface governors** inside Maestro workforce = internal-to-Maestro labeling at a different ontological level. Does not collide with macro-plane vocabulary.
- **mount** = application_execution_relationship, NOT ownership.
- **scars** = operational intelligence accumulated from failure. Preserved, not healed.
- **continuity** = infrastructure, not metadata.
- **substrate** = what compresses out between reasoning and emission. Preservable, not abstract.
- **operator F** = explicit force-closure on a specific claim. Silence is not F.

## Audit Chain (Forensic T2)

RTFA → SEC → SDG → SLR → MMR. Five-document forensic chain producing T2 authority. System 4 outputs. Not auto-promoting; control surfaces.

## What This File Is Not

- Not corpus replacement. Referenced canonical sources remain authoritative.
- Not exhaustive. Vocabulary anchors only.
- Not promotion path. Reading this does not promote anything.
