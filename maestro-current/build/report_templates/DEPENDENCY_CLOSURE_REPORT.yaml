work_item: PROMOTION.2026-09-09
bundle_local:
  interpretation_and_hash_validation: self_contained_manifest_members
  source_templates: build/templates
  report_sources: build/report_templates
external_repository_dependencies:
  - path: artifacts/D-Maestro/Maestro/technical.ust.template.txt
    use: hash-pinned A9b topology compilation
  - path: runtime/maestro-workforce
    use: current worker definitions, ownership/dependencies and existing P0 execution proof
  - path: _PROVENANCE
    use: operator acceptance, supersession, checkpoints and continuation evidence
standalone_limit: >-
  The packaged bundle validates locally; source recompilation and runtime integration
  checks require the declared repository dependencies. Bundle completeness is not
  a claim that it contains every historical source or the executable song runtime.
undeclared_runtime_dependencies: 0
