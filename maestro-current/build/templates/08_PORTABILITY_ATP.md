# Portability Contract & ATP Protocol — Current v0.1 (DEC-11: PORTABILITY.ATP_PROTOCOL)
## Air-Gapped Transfer Pack (ATP)
Contents: UST (canonical, locked) · SEM/SEG report · review transcript · persona participation map ·
manifest + hash. Delivery = verifiable, resumable, auditable. State travels as ATP, never as
freeform context; phantom leakage blocked per R-RT-PD-01. Boot on receipt: continuation integrity
check → verdict (fail-closed) before any drafting.
## MOSAIC interface (P-1)
Maestro consumes MOSAIC as a substrate service behind an explicit contract: substrate provides
target-machine modeling (empirical, domain-neutral) and depth-accumulated heuristics; Maestro
provides no reverse authority — application context/heuristics (SEM config, Suno adapters,
personas) stay Maestro-side (corpus-division v0_3 layered split honored: SEG_GENERIC_ENGINE
patterns may live substrate-side; SEG_MAESTRO_CONFIG and SEG_SUNO_ADAPTER stay application-side).
## Continuation packet (session close)
STATE version + LEDGER delta + OPEN delta + six E's (extension · enhancement · exemplification ·
explanation · impact propagation · application re-optimization — the last two are M14 obligations).
