PROJECT MAESTRO — FULL-CORPUS replay-backed build harvest (production run-book)
Execute against the filestore/compendium D:\maestro-on-mosaic.
PREREQUISITE: the Downloads→untracked backfill + suite install are complete and STATE/LEDGER/OPEN reflect
them (LEDGER HARVEST-INSTALL-1; suite selftest PASS). If not, run that prompt first.

Load _PROVENANCE/STATE.md, LEDGER.md, OPEN.md first. Use the INSTALLED replay-backed-build-harvest skill
(orchestrator) with the maestro-mosaic binding. This run harvests the ENTIRE Maestro/MOSAIC development
record into builder-ready substance for the MVP. Calibration 1 (the Technical-UST adjudication slice) has
PASSED (CALIBRATION_REPORT.md: 15/15, externally_blocked) and was independently V&V'd LAWFUL-WITH-NITS — the
harness is validated. Proceed to the NARROW-START batch, not the full sweep, and STOP for my validation
before widening (M9).

LAWS IN FORCE (govern the whole run; do not violate)
- One-way authority: historical evidence -> forensic reconstruction -> build harvest -> proposal. Never
  retro-edit the reconstruction to fit a build; a product recommendation never flows back into the record.
- Human corrections are ROOT; AI outputs are proposals unless accepted / persisted / runtime-consumed. Never
  silently promote AI inference toward accepted.
- REPLAY LAW (M18): walk each dev thread's Q-A-F cycles as deltas; nothing freezes until I say DONE; keep
  unique, merge duplicates; DELETION needs my explicit statement (never auto-delete); NO verdict / opinion /
  "true solution" until the replay concludes.
- Derivation basis = the ENTIRE project history (M17): S1->S9, T01->T17, located sub-strata, and the
  ~40k-activity context they project from — every stratum consulted where it speaks (DEC-22). Version labels
  are packaging, never authority. Scope verification to the DEC-28 four loss-classes: full SEM, full FOIL
  (Face A + Face B), full agent seat storylines + "act as" defs, and the excellence statements — any thinned
  version is a REGRESSION to flag, not accept. All v5 strata are admissible design evidence (CR-V5-STATUS-1).
- Migration-bundle epistemology (M16): absence from reached material != absence from the system; no "missing"
  verdict without a recorded search; every claim names its lane (KNOWN/PROBABLE/HISTORICAL/UNVERIFIED);
  completeness scopes to the reached strata, never to Maestro.
- Anti-skeleton (fold F2): every emitted category carries >=1 instance with resolvable evidence.
  Registration != application: a ruling is not runtime_enforced until rebuilt through artifacts AND tooling.
- Persistence (M1): nothing operative lives only in conversation; every batch's outputs persist to the store
  (append-only, M2), so the long run can resume.

SOURCE (compendium)
D:\maestro-on-mosaic is the SSOT (DEC-29). Primary transcripts: artifacts/D-Maestro/sources/ (DEC-02).
Strata chain S1->S9 per maestro-current/STRATA_MIGRATION_MAP.md; timeline T01->T17 per the P0 evidence
inventory. Do NOT source from C:\Users\gamer\Downloads (now backfilled into the store). Internal dates are
authoritative; mtimes are unreliable post-consolidation (M7).

BATCHING (M9 narrow-start -> validate -> sweep; DEC-13 chronology oldest-first, small batches; markers are
coverage-only, never an ordering/authority selector)
1. NARROW-START BATCH first: the single most load-bearing UNHARVESTED stratum. Candidate: the Day-4 detailed
   schemas DEC-28 names as the loss locus (full SEM / FOIL / USTs / excellence). Run the full pipeline on it,
   emit outputs, then STOP and show me a coverage + findings summary for validation before any further batch.
2. On my go, sweep the remaining strata oldest-first in small batches; each batch is independently
   reconstructed, harvested, verified, and persisted.

PER-BATCH PIPELINE (the validated slice pipeline, scaled)
a. Source ledger: enumerate + hash + sequence-index the batch's sources (enumerate_sources, index_sequence).
b. Forensic reconstruction: compose maestro-forensic-transcript over the batch -> a FROZEN
   reconstruction_handoff (verify sequence/authority/coverage; if it cannot, mark PROVISIONAL — never fake a
   replay). Emit the honest execution_mode.
c. Intake: emit the work_request; preserve MY original objective verbatim (reframe, don't narrow).
d. Independent first pass (context-isolated, blind to each other): architecture-buildability,
   internal-operation, product-application, and the authority-classifier skill (evidence-authority was
   demoted from an agent to a skill in calibration). Add adversarial-contradiction on the integrated draft.
e. Draft integration -> cross-impact review -> adversarial pass (falsify every "complete / 0-omissions /
   promote-to-accepted" claim; preserve contradictions as typed open_requirement with owner + default +
   escalation; never merge a real conflict).
f. Completion evaluation by the SEPARATE evaluator vs the batch definition of done (never rewrite to pass).
g. Emit the 13 sections as typed records: unique_substance, mutation_record, buildable_component,
   open_requirement, missing_requirement, automation/skill/agent candidates, and a running ai_leverage_plan
   + maestro_mosaic domain_extension. (D5: emit DISCRETE unique_substance[] and missing_requirement[]
   records — do not fold them into a one-liner or into a buildable_component.)
h. Deterministic gates (ALL must pass or log a defect):
   - validate_schema (every emitted record vs its schema);
   - check_evidence_refs (refs resolve; and its D3 LINT must be clean — evidence carries structured
     {ref, resolves} pointers, NOT prose like "UNRESOLVED — …", or the unresolved count isn't auditable);
   - reconcile_coverage (M4/M8: per-chunk operator-delta counts reconciled vs full text; enumerate
     [mediated] decisions + attachment events) AND reconcile_coverage --contradictions (D2: every contradiction
     DECLARED in this batch's handoff is carried/deferred in the batch output — nothing silently drops;
     carry the declared contradiction IDs VERBATIM into the output, or maintain a handoff->output rename map,
     so the reconciliation can match them);
   - check_supersession_propagation (D1 — the gate that makes the OR-STRATA-O18-RESIDUE class catchable at
     scale: for every supersession/ROOT ruling accepted in this batch, scan ALL reached members for the
     withdrawn framing; any unstruck site is an OPEN item, owner operator — determinism/byte-identity is NOT
     currency; report-only, never auto-edit);
   - detect_duplicates, track_unresolved, validate_dep_graph, and the maestro-mosaic binding validator.
i. Persist batch outputs to a harvest lane in the store (append-only) with provenance + a coverage line;
   checkpoint so the run resumes (M1).

ACCUMULATION
Fold each batch's typed records into a corpus-level harvest_result (dedup by content hash — keep unique,
merge duplicates, M18; never silently merge a contradiction). Maintain ONE growing contradiction register
(OPEN) and ONE impact/dependency graph across batches. When a later batch supersedes an earlier finding,
record it BY POINTER with evidence — no "latest wins."

AUTHORIZATION BOUNDARIES (do NOT auto-do — surface as interactive decisions per M6/M12, or as OPEN items)
resolve O16E or any operator-authority contradiction; promote an inferred requirement to accepted product
behavior; force-close / promote a version; delete or merge any stratum; edit accepted _PROVENANCE/canon
state; replace my intent with an AI interpretation. At such a point, PARK and ASK — never guess.

KNOWN BLOCKERS to CARRY (not to resolve)
- OR-O16E-GRANULARITY (Chimera 33/165 vs v5b 28/128 subkey granularity — my ruling).
- OR-SOURCES-UNSTAGED (eaf18e11 compile predecessor + v5b bfe0b238 monolith are named-only — stage them if a
  batch must verify both legs).
- OR-STRATA-O18-RESIDUE (live: STRATA_MIGRATION_MAP.md still ships the withdrawn O-18 line — my ruling to strike).

DEFINITION OF DONE (corpus)
- The replay concludes across all strata (M18): every maestro-current statement verified against the actual
  strata (M17); the DEC-28 four loss-classes verified present, or each thinning flagged as a regression with
  its evidence.
- Every emitted category carries instances with resolvable evidence; every contradiction preserved with an
  owner; every inferred requirement marked INFERRED; execution_mode recorded honestly per batch.
- A corpus builder-ready synthesis + ai_leverage_plan is emitted and persisted; the build_execution_plan
  (adopt-now / prototype / defer / reject / unresolved, smallest working slice, order, tests, migration,
  blockers) is populated from the accumulated records.
- All deterministic gates pass or their defects are logged; STATE/LEDGER/OPEN reflect the run; nothing
  operative lives only in conversation.
- The run ends either DONE (I say so) or at a named external blocker / interactive gate — never on a
  self-declared verdict before the replay concludes.

VERIFICATION PROVENANCE (this run-book itself passed independent V&V, 2026-07-23)
The slice execution was independently verified LAWFUL-WITH-NITS (all 11 substantive laws satisfied). The V&V
found 6 nits; 3 were scale-breaking and are now CLOSED with proven deterministic gates wired into (h) above:
  D1 check_supersession_propagation — proven to catch the real STRATA_MIGRATION_MAP.md O-18 residue.
  D2 reconcile_coverage --contradictions — proven to catch the dropped O-06 (v5c staleness) contradiction.
  D3 check_evidence_refs D3 lint — flags prose-in-ref so the unresolved count is auditable.
Carried dispositions (not blocking, handle in-run): D4 — apply the caps the review already agreed (Engine-A
PROPOSED marker; no single-case ruling promoted to operative) in each batch's records, not just in the agent
guardrails. D5 — emit DISCRETE typed unique_substance[] and missing_requirement[] records per batch. D6 — the
forensic 5-pass REPLAY front-end runs for the first time in the narrow-start batch (calibration validated the
pipeline DOWNSTREAM of the handoff, on the operator's persisted fold); the M9 narrow-start -> STOP-for-
validation gate is the required compensator — do not skip it.

FIRST ACTION
Load state; confirm the backfill + install landed (LEDGER HARVEST-INSTALL-1; suite selftest PASS); select
the narrow-start stratum; run its pipeline; STOP with its coverage + findings for my validation. Use M6
elicitation for any decision (interactive multiple-choice + Other, batches 3-5, <=15/round, context attached).
