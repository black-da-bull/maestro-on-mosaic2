# Maestro-on-MOSAIC — Session Boot (auto-loaded working memory)
This folder is the **single source of truth** for all Maestro/MOSAIC method state (M11/DEC-07). Before doing anything else in any session connected to this folder:

1. Read `_PROVENANCE/STATE.md` (carried-forward working state + rules in force).
2. Read `_PROVENANCE/OPEN.md` (contradictions/unresolved — never silently harmonize).
3. Consult `_PROVENANCE/LEDGER.md` when classifying or citing any change (append-only; supersede by pointer, never rewrite).

## Non-negotiable rules (summary — STATE.md is authoritative)
- **State ladder / rungs:** conversational guidance < proposed < accepted < operative < persisted < runtime-enforced. Every claim of change names its rung and touched dependents.
- **Precedence (M4):** operator conversational input outranks structured AI output on conflict — regenerate the structured output, never the reverse. This rule governs evidence weight; it never limits action.
- **Coverage (M4/M8):** no chunk, summary, or handoff may drop operator deltas; enumerate mutation IDs and [mediated] widget decisions in every handoff.
- **Authority:** operator turns = root; AI turns = proposal until accepted (INV-18 lineage).
- **Elicitation (M6):** operator decisions via interactive multiple-choice + Other, batches 3–5, ≤15/round — never a page of prose questions.
- **Session end (M3):** run the six E's (extension, enhancement, exemplification, explanation, impact propagation, application re-optimization), patch artifacts as patches, append LEDGER, update STATE + OPEN.

Runtime canon (UST/SEM/SEG/FOIL, `artifacts/D-Maestro/canon/`) is a separate register — method files never mutate it.
