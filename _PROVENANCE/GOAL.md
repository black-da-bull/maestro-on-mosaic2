# GOAL — What Maestro-on-Mosaic Is For
**Rung: operative** · v1.0 · 2026-07-27 · Load after `CLAUDE.md` + `_PROVENANCE/STATE.md`, before doing work.
**Audience:** the AI in a fresh session. This file exists so you arrive pointed at the real goal and
stay off the failure path (top-down summarizing, dedup, skeletonizing, premature freeze).
**Authority:** operator intent, ratified by elicitation 2026-07-27 (scope/audience/home/framing).
Reconstructed from the record; every claim below names its source. ROOT (operator) outranks this file
on conflict — regenerate it, never argue from it.

---

## 0. The one sentence
**Teach the machine the Black-Southern-gospel soul that causes emotion — and build only the scaffolding
that soul needs to survive being produced, distributed, and rebuilt without being thinned.**
Everything else in this repo is scaffolding. *The scaffolding exists for the soul, never the reverse.*
[KNOWN — `maestro-current/00_ROOT_SPEC.md` Identity]

Creeds: **"Your vision. Our mission."** · **"Yesterday's world-class is today's baseline."**

---

## 1. Why this exists — the founding wound
Across ~2 years and many AI sessions (T01 2025-05-07 → now), the work was **progressively thinned**.
Operator, verbatim [KNOWN — E1, `sessions/.../eldrik.txt.txt` ~L64399]:
> "so much detail was dropped over sessions that by the end what I thought was a fully developed,
> real-world stress-tested module was not."

Every mechanism here — INV-18 authority asymmetry, phantom-commitment detection, the fold, REPLAY LAW,
this governed filestore — is a countermeasure to that loss. **If you find yourself compressing to save
context, you are re-committing the founding wound.** Don't.

---

## 2. The end (the soul) — never subordinate this
Maestro's purpose is emotional truth in Black-Southern-gospel-rooted music: the thing that makes a
listener feel. The soul is the deliverable. The compiler, the governance, the recovery — all of it is
in service of getting that soul to render intact. When a decision trades soul-fidelity for engineering
convenience, the soul wins. [KNOWN — ROOT_SPEC north-star]

---

## 3. The three stacked goals (the means)
Ordered by dependency. You cannot honestly do #2 or #3 before #1 is real.

### GOAL 1 — RECOVER what was thinned  *(the current gate)*
Replay the **entire** dev-thread history as Q–A–F deltas and recover the four loss-classes that were
progressively dismissed and reduced [KNOWN — DEC-28]:
1. **full SEM** (Song Excellence Matrix), 2. **full FOIL** (Face A + Face B),
3. **full agents** — their whole storylines and "act as" role definitions,
4. the **excellence statements**.
Day 4 holds the detailed schemas the later versions thinned.
- **Procedure:** M18 REPLAY LAW — walk each Q–A–F cycle as a delta; keep unique, merge duplicates;
  **only DELETION requires an explicit operator statement**; **no verdict until replay concludes**;
  **freeze nothing until the operator says DONE.** [KNOWN — M18]
- **Basis:** the entire project history, never a single version. [KNOWN — M17]
- **Treat all v5 strata (v5/-a/-b/-c/.5) as admissible design evidence, not failure.** [KNOWN — CR-V5-STATUS-1]
- **Done when:** `maestro-current` is verified to carry all four loss-classes at full fidelity.
  Any thinned version is a **regression to repair, not a state to accept.** [KNOWN — O16E scope]

### GOAL 2 — EXTERNALIZE Maestro as a shippable platform
Stand Maestro up as an independent software system, not a prompt. [KNOWN — ARCH-SYNTH-1 / ROOT_SPEC]
- **Shape:** Maestro is a **compiler** — creative seed (source) → **Technical UST** (intermediate
  representation) → **Suno** (external renderer / target machine). Suno is *not part of Maestro.*
- **Two engines that never merge:** Engine A forms canonical truth (Input → Expansion → Technical UST →
  Resolution → SEG → SEM → LOCK); Engine B distributes it (LOCK → FOIL → Coverage → Projection →
  Presentation → Renderer). The **Project** is the canonical object that survives forever; the song is
  one projection of it.
- **Runtime loop:** pre-load nulls → blow-up intake (lyric lock) → sequential staffed fill, zero skips
  → congruency round-robin → executive red-pen → loop-until-pass → definitive **LOCK** → **FOIL**
  reverse-pass into the derived surfaces → Suno. **No distribution without linkage** (the v5 loss is the
  canonical counterexample). [KNOWN — ROOT_SPEC Runtime]
- **Ship order:** the workspace-native product (files 00–10 + MANIFEST) ships first and serves as the
  accepted spec + test oracle; the executable app (Rebirth MVP, D1–D4) follows and reconciles to the
  `02` Creative-UST container grammar. [KNOWN — ROOT_SPEC P-4 / STATE next-action 2]

### GOAL 3 — NEVER RE-THIN (the discipline that protects 1 and 2)
- **Persistence:** nothing operative lives only in conversation; if it isn't in `_PROVENANCE/` or a
  runtime-consumed artifact, it doesn't exist next session. [KNOWN — M1]
- **Bundle epistemology:** the repo is a *selected migration bundle*, not the system; absence ≠
  nonexistence; no "missing" verdict without a recorded search; claims name their lane
  (KNOWN / PROBABLE / HISTORICAL / UNVERIFIED). [KNOWN — M16]
- **Self-describing structure:** every significant folder carries its own `CLAUDE.md` declaring axis,
  rung, and what must never happen there. Name-collisions across layers are **signal (thinning
  evidence), not waste** — hash-compare, preserve differing variants. [KNOWN — M19 / DEC-28]
- **SSOT:** `D:\maestro-on-mosaic` is the single source of truth; Rebirth is the executable runner,
  reconciled *to* maestro-current, not the source. [KNOWN — DEC-29]
- **Never silently:** dedup, harmonize a contradiction, summarize evidence in place, or promote AI
  output to canon on re-read. Contradictions go to `OPEN.md`. [KNOWN — M4 / M16 / operating rules]

---

## 4. Where we are right now (verify against STATE before trusting)
**Live objective [KNOWN — STATE 0.7 next-actions]:**
1. **O16E HISTORY-GROUNDED-DERIVATION** — the GOAL-1 gate. Walk the full chronology (S1→S9 / T01–T17)
   as Q–A–F deltas; verify every `maestro-current` statement against the actual strata; hold
   version-only claims at PROBABLE until confirmed; scope verification to the four DEC-28 loss-classes.
2. Reconcile the executable runner (Rebirth MVP) to the `02` Creative-UST container grammar.
3. Re-issue the V&V package (v0.5) for orchestrator re-review; absorb.
4. O-15 prefix registry · O-17 CONS drops · executable app D1–D4 · P2 classification / skill rebuild.
> STATE.md is authoritative for the live ordering. If this section disagrees with STATE, STATE wins and
> this section is stale — flag it.

---

## 5. Non-negotiable laws (fail here and the goal is lost)
No silent fill · no truncation · no revert · nulls fill-or-defend (null = reserved address, not
absence) · lyric lock (quoted lyrics immutable after intake) · every decision traceable · contradiction
is a feature (registered, never harmonized) · ROOT outranks PROPOSAL; only explicit operator acceptance
promotes to canon · era-bound authority · interactive gates, never passive stops · derivation basis =
the entire history, never a version · bundle ≠ system. [KNOWN — ROOT_SPEC Laws]

---

## 6. The failure path — what NOT to do (this is how the wound recurs)
- ❌ Read top-down, summarize the middle, or compress to manage context. The middle **is** the work.
- ❌ Dedup by filename/size. Different content behind the same name is the thinning trail (GOAL 1).
- ❌ Emit a verdict, a "current state," or a freeze before the replay concludes or before the operator
  says DONE.
- ❌ Treat your own prior output as canon on re-read, or harmonize a contradiction to make things tidy.
- ❌ Treat v5 as a failure to discard. It is design evidence.
- ❌ Build GOAL 2 on an unrecovered GOAL 1 — you'd be shipping the thinned version.

---

### Provenance
Sources read for this file (2026-07-27 Cowork session): `00_ROOT_SPEC.md`, LEDGER (E1–E4, M1, M4, M16,
M17, M18, M19, DEC-28, DEC-29, CR-V5-STATUS-1, ARCH-SYNTH-1), STATE v0.7, OPEN, `P0_EVIDENCE_INVENTORY_v0_1.md`,
`EXECUTIVE ARCHITECTURAL SYNTHESIS.md`. Framing ratified by operator elicitation 2026-07-27
(soul-as-end; layered scope; boot audience). Lane: goals 1–3 and the north-star are KNOWN (operator-
sourced); the "where we are" section is only as current as STATE — re-verify each session.
