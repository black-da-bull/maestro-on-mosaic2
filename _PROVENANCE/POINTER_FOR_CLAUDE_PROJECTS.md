# MAESTRO STATE POINTER — paste into project knowledge of both design projects
(For: "MoMoney Studios Design System" and "Copy of MoMoney Studios Design System")

---

**This project is an external surface, not a source of truth.**

All Maestro/MOSAIC method state — mutation ledger, carried-forward working state, open contradictions — lives in the operator's local folder `D:\maestro-on-mosaic\_PROVENANCE\` (STATE.md, LEDGER.md, OPEN.md). Do not create or maintain doctrine/method documents in this project; anything method-level produced here is a **proposal** to be exported to that folder and ruled on there.

Rules in force here regardless:
1. **State ladder / rungs:** conversational guidance < proposed < accepted < operative < persisted < runtime-enforced. Name the rung for any claimed change.
2. **Authority:** operator turns are root; AI output is proposal until the operator accepts it. On conflict, operator conversational input wins — regenerate the structured output, never the reverse.
3. **Cross-context propagation is not automatic.** Artifacts made here (e.g. `campaigns/run-it-to-me/` covers) exist only here until physically exported to `D:\maestro-on-mosaic\projects\design-system\`. When creating deliverables the operator will need elsewhere, say so and offer the export.
4. **Operator decisions:** interactive multiple-choice + Other, batches 3–5, ≤15 per round — not prose question walls.
5. At session end: list what was produced, which rung each item reached, and what (if anything) must be exported to the SSOT folder.

Provenance: established 2026-07-16, Cowork reconstruction session (LEDGER M11/DEC-07). Worked example that motivated this pointer: the "Run It To Me" covers — persisted in this project, invisible from every other context.
