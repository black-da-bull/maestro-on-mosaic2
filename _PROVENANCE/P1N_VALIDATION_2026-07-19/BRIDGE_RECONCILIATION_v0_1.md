# Bridge Reconciliation v0.1 — Two Parallel Assessments, One Merged Work Item (PROPOSAL)
**Date:** 2026-07-19 · **Author:** Cowork session (R2 validator) · **Class:** AI proposal (INV-18); append-only; supersessions by pointer only.
**Inputs:** `Human - Maestro v5b Workspace Discussion.md` (repo root, md5 `334e6e0eb9c0a37855adaf57ca37af3f`; PDF twin `71fcc25e4e2ce3e172842d0b522205b6`) · my assessment `ASSESSMENT_R1_R2_AND_PIPELINE_v0_1.md` · R1/R2 validation files · `_PROVENANCE` state.

## 1. What the new file is (provenance)
The v5b Workspace Discussion export is the orchestrating session's transcript: the operator's foundational-assumption correction (the M1 origin text) → the nine-expert panel prompt and report → R1's displayed response → this session's (R2's) displayed responses → the operator's workspace clarification → the refined ASSESSMENT REQUEST → the orchestrator's own assessment ending in `REPAIR.P1N.SESSION-TO-MAESTRO-BRIDGE.R1`. Operator turns inside it are ROOT; the AI reports inside it are analysis-class. It sits at repo root and is **not yet registered in INDEX.json** (4,305 records predate it) — registration proposed below. It also documents that the ASSESSMENT REQUEST answered by this session was authored by the orchestrator refining the operator's prompt, and that the orchestrator answered the same request in parallel.

## 2. The reassessment headline: a two-witness result at the recommendation layer
Two assessments were produced in parallel from the same request by different systems in different workspaces — this session (cold side, hands on the primaries) and the orchestrator (continuity side, synthesizing). They converge on every load-bearing conclusion: R1 = continuity witness / R2 = portability witness, asymmetric pair stronger than either doubled; the narrow replay-validation stage is closed; corpus-wide T01–T13 replay now would create "many validated mutation records but no validated path into the product"; the next step is bridging the session-13 FIRM cluster into a candidate Maestro update; prerequisite method patches are the same list (splitter script, normalization spec, attachment + mediated enumeration, quote convention, word-boundary sweeps, density marker set); the operator is the sole acceptance authority. Independent convergence of this strength is itself evidence the repair direction is right.

## 3. Where the orchestrator's BRIDGE.R1 improves on my E2E-01 — adopted
1. **Earlier stop.** BRIDGE.R1 halts at the candidate update package, before operator application. That keeps the automated portion objectively PASS/FAIL-able — my E2E-01 embedded operator acceptance inside the work item, making its verdict partly non-mechanical. Adopted.
2. **Cold-regeneration pass criterion.** "A separate workspace can regenerate the candidate package from persisted inputs" promotes the R2 lesson into the pipeline and directly attacks the regeneration gap both assessments flagged as unproven. Adopted.
3. **Target routing with unknown-stays-unresolved.** No generic Maestro bucket. Adopted.
4. **Candidate-state application area.** Apply accepted patches to a branch / worktree / designated candidate directory, never onto the evidence corpus. The repo has `.git`; a branch is feasible. Adopted for phase 2.
5. **Role table as contracts.** Ten roles each with a must-not-do column — adopted as *skill-level contracts convened per stage*, not a standing staff (per the panel's own Expert-9 warning and the reconciled dissent).

## 4. Where my E2E-01 adds what BRIDGE.R1 lacks — carried forward
1. **The loop still needs an objective end-state gate.** BRIDGE.R1's PASS leaves Maestro unchanged — the loop is proven only to the doorstep. The V1 re-sweep flip (the same word-boundary persistence sweep that today returns zero must, after application, FIND the FIRM canon in persisted artifacts, every entry source-anchored, rungs named in LEDGER/STATE/OPEN) is the objective proof of loop closure. It belongs in a scheduled phase 2, not dropped.
2. **Stage-9 honesty.** The orchestrator's validation stage lists fixtures, lyric-lock tests, and compiler-cap tests that presuppose an executable runtime. v0-on-Mosaic is specified, not built; v4.5.5 is proven but lives as conditioning + chain, not as locally executable code. The bridge test plan must therefore split **executable-now** checks (schema validity, cross-reference integrity, anchor verification, contradiction preservation, diff reversibility) from **deferred-until-runtime** checks (song fixtures, lyric-lock, compiler caps), each deferred item carried as an explicit open obligation. Otherwise the bridge fails on tests it can never run — a false FAIL is as corrosive as a false PASS.
3. **The model-family concern resolves through tooling, not testimony.** R1/R2 are same-model-family witnesses; the durable answer is the one the prerequisite patches already imply — move verification into persisted, model-agnostic scripts. Once the splitter and witness checks are scripts in the repo, any workspace, model, or human can run them; the witness stops being a model's word.

## 5. Reconciled plan (supersession by pointer)
`REPAIR.P2E.S13-TO-MAESTRO.E2E-01` (my prior G item) is **superseded by pointer**, split into:

**Phase 1 — `REPAIR.P1N.SESSION-TO-MAESTRO-BRIDGE.R1`** (orchestrator's item, amended): prerequisite method patches → target routing → impact graph → staleness map → candidate patchset (minimal, reversible, source-anchored, contradictions visible) → test plan split executable-now vs deferred-until-runtime (amendment 4.2) → bounded expert adjudication → operator decision packet → **cold-workspace regeneration of the package from persisted inputs** → verdict. Stop. No operative file changes.

**Phase 2 — `REPAIR.P2E.S13-APPLY.R1`** (new, runs only on operator acceptance of the phase-1 packet): apply accepted patches in a branch/candidate-state area → executable-now validation suite → operator force-close → persist to canonical homes → **objective PASS = V1 re-sweep now finds the FIRM canon, 100% entries anchored, LEDGER/OPEN/STATE updated with rungs named** → continuation packet distinguishing source / accepted / candidate / persisted / runtime-enforced state. Stop. Corpus replay (oldest-first, small batches, carrying validated operative state forward — the orchestrator's sequencing, adopted) begins only after phase 2 passes.

## 6. Housekeeping proposals (operator ruling)
- Register `Human - Maestro v5b Workspace Discussion.md` (+ .pdf) in INDEX.json with the md5s above; classify as the orchestrating-session export (T-series extension beyond T17).
- Add pointer lines for R1, R2, the two assessments, and this reconciliation to LEDGER (pointers, not doctrine copies — DEC-07).
- Density metric: per the orchestrator's alternative, either persist the marker set or **retire density as ordering authority** for future session selection; ruling needed before the T01–T13 sweep uses it.
