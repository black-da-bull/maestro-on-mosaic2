# P1N Validation — R2 Second Witness
**Work item:** `REPAIR.P1N.PANEL-VALIDATION.R1` · **Run:** R2 (second independent witness) · **Date:** 2026-07-19 (04:09 UTC / 2026-07-18 23:09 CT)
**Verdict: PASS — concurs with R1.** Every mechanical claim of the 2026-07-19 R1 validation that R2 tested reproduced from the primaries. No claim was promoted on trust; R2 re-derived everything below from source, not from R1's self-description.

**What R2 is:** a third session in the chain (replay session → R1 validator → this). Separate context from both. Same caveat both prior artifacts already concede: same model family, same corpus — independence is of derivation, not of witness kind. R2 deliberately varied its methods where possible: a different hash-sample stride (89 vs R1's 107, disjoint samples), its own probe set, its own omission sweeps, and a persistence sweep over a *wider* scope than R1 declared.

---

## 1. Provenance — reproduced
Both primaries hash exactly as recorded: `claud3.txt` md5 `faecdb76…` (407,408 B), `claude-session13-window2.txt` md5 `94989f7f…` (927,063 B). DEC-02's three-names-one-file equivalence verified by digest across all four W1 copies; W2 archive copy identical to primary. INDEX.json holds exactly 4,305 records; an independent 49-file sample (stride 89, disjoint from R1's 41-file stride-107 sample) verified 49/49 against recorded MD5s — across R1+R2 roughly 90 distinct records plus the session-13 set now hash-verified, zero failures. The recovered kernel is byte-identical (md5 `884af3ed…`) to `KERNEL.md` inside `archive/im_dead_kb.zip` (zip-dated 2026-05-20).

## 2. Coverage — reproduced to the digit
Split re-verified end-to-end: 36/58 segments, contiguous, zero gaps, every segment's text equal to its offset slice, concatenation equal to the CRLF→LF-normalized source (393,821 / 914,340 chars; 6,418 / 8,647 CR removed) — every number identical to R1's COVERAGE.json. 17+32 user segments, indices matching; all present in the operator-verbatim files. REC-A verified at W1 assistant-seg-2 tail (raw byte 19,654); REC-B (R-LIST v0.1 paste, 9:21 PM) at seg-18 tail (byte 216,190) with the K-2 attribution in seg 19. Independent sweep of all 43 assistant segments: 55 embedded `You said:` strings, all quotations of the *inner* 4_5_5.txt transcript (line-numbered grep output, parser code, thinking text) — **zero further session-level operator inputs**, same result as R1. The W2 seg-16 attachment chrome (`4.5.5.txt / txt / 2:15 PM`) is present — MF-3 confirmed.

## 3. Mutations — reproduced
61 mutation IDs, unique and contiguous (27+34); footer tallies internally consistent (W1: 24 operative · 1 operative→superseded · 2 accepted-unresolved; W2: 23+6+2+3). All high-authority probes found verbatim in source: the U13 flagship at **byte 107,525 exactly**; U41 MODE canon; U39 rebirth line; U51 vis.vig correction; U56 authority/phantom locks (`R-RT-QAF-01` ×10, `R-RT-PD-01` ×7 in W2); U57 triangulation; U32 pre-Mosaic ruling; both `Claude's response was interrupted.` markers; the U14 promotion order. The R-list v0.2 totals block reproduces **with its category arithmetic**: 14 categories (R-MS 8 … R-INV 5) summing to 96, FIRM 92, CAND 3, OPEN 1 — present in both windows in column-padded form, which R2's naive exact-string probe initially missed and a whitespace-insensitive probe found: **MF-1/MF-4 (normalization and quote-flattening caveats) independently confirmed as real and as harmless.**

## 4. Contradictions — reproduced
9/9 with both sides traced to source text (CR-1..4, C1..5), held-open items carried (CB-02/04/06, OPEN-Q5..Q8). R2's own correction-marker omission sweep (different marker set from R1) found every marker-bearing operator turn already covered in the replay; no unregistered corrective turn. Nothing silently harmonized — CR-2's undischarged promotion order is preserved on both sides.

## 5. Persistence — reproduced, scope widened, one sweep-hygiene finding
Zero genuine occurrences of `R-RT-QAF-01`, `R-RT-PD-01`, `Config B`, or any R-list reference outside the session-13 transcripts and `_PROVENANCE` — searched: `canon/`, `drafts/`, `HYDRA/`, `system/`, `untracked/`, `styles/`, root docs, `ws_*.yaml`, and (wider than R1's declared scope) the **full `code/` tree**. One raw hit surfaced and was inspected: `code/…/legacy/context.md` line 545,373 — "Mini-**Config B**lock", a substring false positive. **R2 finding (one, method-class): persistence sweeps should use word-boundary patterns; recorded as MF-7 candidate.** Once inspected, the hit strengthens V1 rather than weakening it. Canon staleness stands: zero `v4.5.5` references in any text-searchable canon file (zip/pdf residual gap MF-5 stands). MF-6 confirmed at the line level: `drafts/maestro_v0.md` lines 184–185 define K7=Arrangement/K8=Commercial against session 13's FIRM K7=External viability/K8=Visual coherence. INV-18 lineage confirmed: original at claud2.txt byte 252,444, Revised at 255,842, and the installed `maestro-forensic-transcript` skill is SKILL.md-only and carries the **original** authority framing — Revised INV-18 absent (O-02 stands), provenance line still unannotated (O-01 stands). `projects/` absent (O-12 pending). Newest content touch tree-wide: `untracked/website/…2026-07-19 01:53` (c03 stands).

## 6. Not re-verified (unchanged from R1, honestly scoped)
The correction-density metric (MF-2) — marker set still unpersisted; R2 did not re-derive it. T14 recount (c07) — originals live outside this corpus. The splitter script itself is still absent (O-14); the split is validated by its outputs, not re-executed.

## 7. Verdict and stop
**PASS, concurring with R1.** Two independent derivations from the primaries now agree on every testable claim. Per the work item this authorizes staged replay of the next small session batch — an authorization that remains the operator's to exercise, with MF-1..MF-4 folded into the method, word-boundary sweep hygiene (this run's finding) added, and O-14 splitter repair still due first.

Stopped at verdict. No Maestro runtime artifact, LEDGER, STATE, or OPEN file was created, modified, or patched. R2's only writes are the four files in `_PROVENANCE/P1N_VALIDATION_2026-07-19/R2_SECOND_WITNESS/`.
