# Session Reassessment under the Migration-Bundle Epistemology (M16) — v0.1
**Work item:** MAESTRO.O16D.MIGRATION-BUNDLE-CORRECTION.R1 · 2026-07-19 · rung: PROPOSAL
(operator acceptance promotes; nothing here self-promotes)
**Constraint applied:** the supplied files are a selected migration bundle applied to a
~40k-activity system. Absence ≠ nonexistence. Each artifact = state-changing input implying
prior state + change + derivative documents. Prior assistant output is non-authoritative unless
accepted or migrated. Every claim below names its lane.

---

## 1. Where this session mistook the bundle for the system — specific instances, corrected

**Instance 1 — the seat map.** v0.3 shipped stating the named-worker seat map was
"blocked on CONS drops" — a nonexistence claim built from not having reached
`legacy/maestro_v5b_coldstart/`. The pinned matrix (PIN.V5B.COUNCIL.MATRIX.V1) was in-repo the
whole time. CORRECTED: imported into 04 (hash-verified, PER-coded, MAP row scope-split into
PER.execution).

**Instance 2 — the topology.** The O16C compiler treated the Chimera-era master template as
"the" canon skeleton and reported "0 omissions." The v5b coldstart pack contains a SECOND
topology monolith (TECHNICAL_UST_CANON.md — 8 era-axes, ~128 subkeys, different granularity,
LYR.K0 governance keys, THY.K5 tension–release curve) plus a condensed Gemini-print address map.
"0 omissions" was true of the source, silently presented as true of the system. CORRECTED:
claims re-scoped (SCOPE_CORRECTION_v0_4), competing stratum registered, adjudication = O-18.

**Instance 3 — the CONS queue as a nonexistence register.** Items were queued as
"cannot migrate what is not in the repo" without a recorded repo-wide search. Two queue items
(pinned matrix, full-topology file) were in the repo. CORRECTED: every remaining CONS entry now
carries a verified-absent-by-search burden.

**Instance 4 — displaced ≠ missing.** runtime/context.md and runtime/solution.md were "absent"
from the coldstart pack — actually present at other paths under Windows duplicate-suffix names,
provable only by content hash against the pack's own MANIFEST.json. The bundle healed its own
scatter. Method gain: manifest-hash re-binding is now a standard recovery move.

**Instance 5 — the unextracted PDF.** The v5-b ADDRESS MAP PDF sat staged and unprocessed while
conclusions about "the pinned address map" were being drawn; it is an image-only Gemini chat
print (2026-01-23, same date as the pack) — extractable all along via page rendering.

## 2. Known canon (operator-ruled or hash-verified in reached strata)
M1–M16 · DEC-01..26 · P-1..P-4 · the 7-axis re-expanded topology top (DEC-26) · M13 Roadmap
functional migration · SEM 12-criteria / 97.5 floor / G-Card artifact / interwoven execution
(DEC-24) · lyric lock (words locked, spacing transformable) · style-conditioned breath-pattern
rule (DEC-25) · chronology-authority + overlay law (DEC-22/23) · the Session-13 replay-backed
snapshot (historical class) · the v5b coldstart pack AS A STRATUM (its content is era-canon of
2026-01-23, hash-verified; its authority in current canon flows only through DEC-22 chronology
and operator rulings).

## 3. Probable canon (multi-witness, unruled — held at PROPOSAL)
Precise triad cap bands (4960–4999 / 960–999 / ≤150+1960–1999 — v5b pack + validator band +
DISC-04 rounding pattern, three witnesses) · Suno parsing discipline rules (v5b; consistent with
the v4.5.5 staging floor and no later contradiction) · evidence-contract fill law (one-sentence
constraint + source binding + downstream prediction + challenge cycle) as the executable form of
"staffed fill" · work-item mechanics (WI.V5B.SCHEMA.V1) as ancestry of the 03 phase chain.

## 4. Historical (era-bound overlays, preserved, non-current)
MAP as an axis and the Creative [Road Map] parity patch (v5b) — removed by M13, both records
kept · G-Card ≥7.0 scalar gate · fixed 6–10/6–11 syllable bands · four-letter Performance code ·
SEG expansions "Feasibility gate" / "Structural Excellence Gate" / "Structural & Engineering
Gate" · v4.5.5 production roster (PROVEN era) · 5-council default + 13-worker lattice as
stratum rosters · SE20-as-address-group (Gemini print) and SE20.K1–K7 item set (v5b instance).

## 5. Unverified (located-unfolded, or inferred-only — no claims built on these)
The ~40k-activity runtime history (mostly not in evidence) · remaining coldstart docs folded
only at registration depth (LOG_TEMPLATES, MODEL_PROFILE degradation rules, DRY_RUN_EXAMPLE) ·
05_SEM_MUTATION_TRANSCRIPT_REVISED.md (sessions lane — located, unread) · the true derivation
relationship between the Chimera-era template and the v5b monolith (which is upstream of which
in authoring fact, as opposed to file dates) · everything in the CONS queue.

## 6. Conclusions from earlier in the session that REMAIN VALID under M16
The V1 flip (FIRM identifiers now persisted — before/after sweeps are searches, not absence
claims) · R1+R2 dual-witness replay validation (hash-anchored to payload md5 52381c1e…) ·
the preflight parse-gate discipline and its candid self-catches · the A9a/A9b builder split and
byte-stable rebuild · lyric-lock sequence+multiplicity comparator · the K-namespace collision
finding (two K-sets; prose qualification until O-15) · M13/M15/DEC-22..26 rulings and their
application · the session→Maestro loop having executed once end-to-end.

## 7. Conclusions SUPERSEDED or re-scoped
"Loop closed" → loop executed once, session-to-validated-persistence (orchestrator correction,
already absorbed) · "0 omissions" → zero omissions relative to the named source · "seat map
blocked on CONS" → false, imported · "complete bundle" language anywhere → completeness of
reached strata · v0.3 RC → v0.4 RC2 (force-close hold widened).

## 8. Assumptions that were never canon (and stay that way)
That the earliest-found topology source was the only one · that a file absent from reached
strata is absent from the repo · that a package listed in a manifest but not at its path is
lost · that AI-generated era documents (the Gemini print, GPT-era pack prose) carry authority
beyond their stratum without operator acceptance — they never did and do not now.

## 9. Contradictions still requiring resolution (operator)
**O-18** topology detail-stratum (Chimera-era 33/165 vs v5b 28/~128; layering rule vs single
source) + V&V Marshal ↔ STAFF.ROLE.GOVERNANCE correspondence · **O-15** prefix registry (now
including the v5b pinset IDs) before any K-label binding · **O-17** CONS drops under the
search-burden rule · force-close of maestro-current v0.4 · SE20 composition OQ (held) ·
sub-gate threshold OQs (70v75, SEM 12v13) — all held, none harmonized.

## 10. What the correction changes going forward (method, not just repairs)
Search-before-absence is now a gate, not a virtue. Completeness statements carry a scope clause
naming the strata they cover. Every located artifact triggers the three questions (prior state?
change? derivative documents?) before classification. The CONS queue is a claims register with
evidence burdens, not a parking lot. And the calibration case for M16 is this session itself:
the corrector was corrected by its own rule, found the counterexample inside its own workspace,
and the repair produced net new canon (seat map, governance address space, cap bands) rather
than only retractions — which is the intended behavior of a fold that builds upon, not replaces.
