# Method Patches — folded before bridge execution (2026-07-19)
Prerequisites of REPAIR.P1N.SESSION-TO-MAESTRO-BRIDGE.R1. Each patch names its source finding. Scripts beside this file are the persisted, model-agnostic form — any workspace, model, or human can run them (the durable answer to the same-model-family witness concern).

1. **MF-1 — Normalization spec.** The mechanical split is *character-lossless after CRLF→LF normalization* (the split's only transformation). All segment offsets index the NORMALIZED text; raw-byte anchors are given separately where used. "Byte-lossless" is retired wording.
2. **M8 + MF-3 — Coverage enumeration.** Coverage counts enumerate: (a) verbatim user turns, (b) [mediated] tool-elicited operator decisions (cited via the carrying assistant turn, never paraphrased as verbatim), and (c) **operator attachment events** (file-attach chrome, e.g. `4.5.5.txt / txt / 2:15 PM`) as operator EVENTS.
3. **MF-4 — Quote-normalization convention.** Replay quotes may flatten line breaks / column whitespace of column-formatted source blocks; content is verbatim, layout is not. Any consumer matching quotes against source MUST use whitespace-insensitive matching. Payload-class extractions (e.g. PATCH-01) preserve layout exactly.
4. **MF-7 — Sweep hygiene.** Persistence sweeps use word-boundary/anchored patterns. Precedent: naive `Config B` matched "Mini-**Config B**lock" (`code/.../legacy/context.md:545373`). Every raw hit is inspected before counting. Script: `persistence_sweep_v0_1.py`.
5. **O-14 — Splitter (mitigated, not closed).** The original splitter script remains unrecovered. Persisted now: `s13_split_validate_v0_1.py`, which mechanically PROVES a segments.jsonl against its source (contiguity, zero-gap, slice-text identity, concat==normalized source, role census). Regeneration parity for future splits requires either recovering the original script or adopting this validator as the acceptance gate for any new splitter. O-14 stays OPEN for the script itself.
6. **MF-2 — Density (disposition pending, elicitation E-5).** The marker set behind the original session-ordering density figures was never persisted and is not reconstructed here (would be retroactive invention). Persisted instead: `correction_markers_v0_1.py` — the R2 omission-sweep marker set (a different instrument, fit for coverage sweeps, NOT a reconstruction of the ordering metric). Operator ruling: persist a fresh density definition or retire density as ordering authority.
7. **DISC-09 (held for elicitation E-6).** Era authority inversion — "Documents are primary truth; chat is telemetry" (v4.5.2 canon doc, KBD-001) vs the current runtime-primary hierarchy. Proposed supersession statement text is in the decision packet; nothing recorded until ruled.
