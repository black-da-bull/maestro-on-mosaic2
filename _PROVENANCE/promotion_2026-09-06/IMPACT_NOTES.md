# Dependency analysis before source repair

Authority: operator DEC-PROMO-01–13; repository README explicitly denies authority by folder/name alone.

- SEM numeric claims: templates 01/05/06/10/STRATA → generated counterparts → MANIFEST; bundle validator A5 floor assertion → corrected configuration validation + regression tests; executable-app draft → corrected downstream specification.
- Evaluation capability remains in 05; formula and independent hard gates preserved. The session policy input vocabulary exists in Activity.txt M7.3 (line 401); its later default is historical and explicitly not imported.
- Technical→Creative: 01 threshold invariant changes; 02 and 03 authority inspected, VERIFIED_NO_CHANGE. No renderer canonicalization.
- Workforce: 04 audio roster and visual-module separation VERIFIED_NO_CHANGE. Historical rosters do not expand current audio count.
- Q1–Q16/O-03/O-13/O-04/O-05/O-12: OPEN chronology retained; append current fold; STATE stale summary superseded. Other historical open issues retained without inventing closure.
- Suno: existing target-machine shorthand in 00 receives a boundary clarification only; no adapter implementation. Seedance: no current bundle claim to patch; experiment classification persisted in current continuation/state.
- New related stale claims: 00/STRATA contradict later CR-V5-STATUS-1 and DEC-27 about v5 and O-18; current interpretation repaired; original snapshots untouched.
- Existing builder copies 13 templates and packages build/compiled/fixture trees. compile_current.py separately generates three topology members from the hash-pinned predecessor; rerun and compare these before full packaging. No manual generated-document patching.
- Current internal references checked against full pinned tree, exact bundle members, and explicit source aliases; historical import locations are resolved by recorded full paths, not presumed absent.

Every matched artifact and every current bundle member is enumerated in IMPACT.csv; every matched occurrence is in OCCURRENCES_BEFORE.csv.gz. The inventory includes opaque historical binaries and the external gitlink, whose objects remain unchanged. An unexamined historical object cannot support a complete text-occurrence audit; final validation must disclose any remaining retrieval gaps.

After scan: OCCURRENCES_AFTER.csv.gz; current-only readable excerpt: CURRENT_THRESHOLD_OCCURRENCES.csv. Gzip streams are deterministic (mtime=0); Python gzip or gzip -dc reconstructs the full CSVs. Every report-created echo of an older claim is evidence of this correction, not a newly applicable policy.
